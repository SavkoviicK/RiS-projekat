package com.veterinarska.stanica.service;

import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;

@Service
public class EmailService {

    private final JavaMailSender mailSender;

    public EmailService(JavaMailSender mailSender) {
        this.mailSender = mailSender;
    }

    public void posaljiTestEmail() {
        SimpleMailMessage poruka = new SimpleMailMessage();
        poruka.setTo("primac.test@gmail.com");
        poruka.setSubject("Testiranje mail servisa");
        poruka.setText("Ovo je test poruka iz projekta Veterinarska stanica.");
        mailSender.send(poruka);
    }
}
